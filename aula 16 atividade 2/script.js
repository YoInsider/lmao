function takeValue() {
    item = window.document.getElementById("item").value
    list = window.document.getElementById("items")
    list += item
    showResult(list, item)
}

function showResult(list, item) {
    
}